# 《项目文档》

## 1. 项目概述

### 1.1 项目名称

数据归档调度系统（Archive Orchestrator）

### 1.2 建设目标

本项目使用 Go 构建一个可独立部署的数据归档调度系统。系统面向需要定期清理在线数据、降低主存储成本，同时又必须保证历史数据可追溯、可校验、可恢复的业务场景。

系统以“保留规则”为核心：管理员定义数据保留周期、归档范围、执行频率、目标存储和重试策略，后台调度器据此生成归档任务。任务依次完成数据扫描、归档写入、完整性校验和源数据清理；归档失败时自动重试，超过阈值后进入人工处理状态。用户还可按归档批次发起恢复任务，将历史数据恢复到指定位置，并通过审计记录追踪所有关键操作。

### 1.3 目标用户

- 运维管理员：维护保留规则、观察任务状态、处理失败任务。
- 数据管理员：查询归档批次、执行完整性校验、发起数据恢复。
- 审计人员：查看任务和操作日志，确认数据生命周期符合要求。
- 上游业务系统：通过 API 创建规则、查询任务及发起恢复。

### 1.4 项目边界

系统负责归档流程编排、任务状态管理、元数据持久化、文件归档适配、校验、恢复及失败重试。首版使用本地文件系统模拟源数据与归档存储，便于独立运行和验收；存储接口保留扩展能力，后续可接入对象存储或数据库。系统不负责生成业务源数据，也不提供复杂身份中心和多租户计费功能。

## 2. 核心业务概念

### 2.1 保留规则

保留规则描述“哪些数据在什么时间以什么方式归档”。规则至少包含：名称、源目录、文件匹配模式、保留天数、归档目录、调度表达式、校验算法、单批最大文件数、重试次数、重试间隔、启停状态及版本号。

规则启用前必须校验路径、表达式和数值范围。已被任务引用的规则不直接物理删除，而是标记为停用，确保历史任务仍能展示当时使用的规则快照。规则更新采用乐观锁，防止多人同时修改时发生静默覆盖。

### 2.2 归档任务

归档任务是一次规则执行实例，保存规则快照、计划时间、实际开始时间、结束时间、状态、尝试次数、文件数量、字节数、校验结果和错误原因。每个计划时间只能生成一个任务，以幂等键避免调度器重启后重复创建。

### 2.3 归档批次与清单

任务执行成功后形成一个不可变归档批次。批次清单记录每个源文件的相对路径、大小、修改时间、归档位置和校验摘要。清单既是校验依据，也是恢复时的输入。批次被恢复或重新校验不会改变原始清单，只追加新的操作记录。

### 2.4 恢复任务

恢复任务从指定归档批次读取清单，将全部数据或按路径筛选的数据恢复到目标目录。恢复前检查目标路径安全性和可用空间；遇到同名文件时按用户选择执行跳过或覆盖。恢复完成后再次计算摘要，并记录恢复成功、跳过和失败的文件数。

### 2.5 任务事件与审计日志

任务事件用于描述状态流转和执行细节，例如任务领取、文件扫描完成、归档写入、校验失败及重试调度。审计日志记录操作者、时间、动作、资源类型、资源标识、请求标识和结果，用于追踪规则变更、手工重试、取消与恢复操作。

## 3. 功能需求

### 3.1 保留规则管理

1. 创建、查询、分页列出、更新、启用和停用保留规则。
2. 校验源目录不能等于或包含归档目录，避免归档文件被再次扫描。
3. 支持通配模式筛选文件，并按文件最后修改时间判断是否超过保留期。
4. 支持固定间隔调度，最小间隔为 1 分钟；保存时给出下一次执行时间。
5. 对规则更新执行版本检查；版本冲突返回明确错误。
6. 启用规则后立即参与调度，停用规则后不再产生新任务，但不影响已运行任务。
7. 支持手工触发规则；同一规则已有运行中任务时拒绝重复触发。

### 3.2 自动调度

1. 后台调度器按固定轮询周期加载已启用且到期的规则。
2. 针对规则标识与计划时间生成唯一幂等键，在事务中创建任务并推进规则的下次执行时间。
3. 多个工作协程竞争任务时，通过数据库租约保证同一任务只被一个工作协程执行。
4. 工作协程定期续租；进程异常后，过期租约可由其他工作协程重新领取。
5. 支持优雅停机：停止领取新任务，并允许正在处理的任务在超时范围内结束。

### 3.3 归档执行

1. 扫描规则指定目录，过滤目录、符号链接、不匹配文件和未超过保留期的文件。
2. 对候选文件排序并限制单批文件数，保证超大目录可分批处理。
3. 先将文件复制到批次临时目录，逐个计算 SHA-256 摘要并写入内存清单。
4. 清单以 JSON 格式写入临时目录，随后以原子重命名方式发布为正式批次。
5. 发布后按清单重新读取归档文件并校验大小和摘要。
6. 只有全部文件通过校验后，才允许删除源文件；删除失败时任务标记为部分失败并进入重试。
7. 执行过程中持续更新文件数、字节数和阶段事件，便于界面展示进度。
8. 临时目录残留不会被视为有效归档；后续任务可识别并清理属于自身的残留数据。

### 3.4 完整性校验

1. 归档任务默认执行即时校验，确保清单与归档内容一致。
2. 用户可对历史批次发起独立的重新校验。
3. 校验结果区分通过、文件缺失、大小不符、摘要不符和读取失败。
4. 校验失败不修改原始清单，结果作为新的任务和事件持久化。
5. API 返回汇总结果，详细错误可通过任务事件查询。

### 3.5 数据恢复

1. 用户选择归档批次、目标目录、路径筛选条件和冲突策略后创建恢复任务。
2. 系统验证目标路径必须位于配置允许的恢复根目录内，阻止路径穿越。
3. 恢复采用临时文件写入、摘要校验和原子重命名，避免暴露半写入文件。
4. 默认冲突策略为跳过；覆盖模式必须由请求明确指定并写入审计日志。
5. 单个文件失败不影响其他文件恢复，任务结束时根据结果标记成功或部分失败。
6. 恢复操作不会自动删除归档数据，归档批次保持不可变。

### 3.6 失败重试与人工处理

1. 扫描、复制、清单写入、校验、源文件删除和恢复阶段产生的可恢复错误进入自动重试。
2. 重试间隔采用指数退避，并设置最大等待上限；规则可配置最大尝试次数。
3. 参数错误、路径越界、清单损坏等确定性错误直接标记为永久失败。
4. 每次尝试均记录开始时间、结束时间、错误分类和错误摘要。
5. 超过最大尝试次数后任务进入“待人工处理”状态。
6. 管理员可修复外部问题后手工重试，手工重试保留原任务历史并创建新的尝试记录。
7. 等待执行或等待重试的任务可取消；运行中任务收到取消信号后在安全检查点退出。

### 3.7 任务查询与统计

1. 按任务类型、状态、规则、创建时间筛选并分页查询。
2. 查看任务详情、规则快照、执行统计、尝试记录及状态事件时间线。
3. 首页展示规则总数、运行中任务数、最近 24 小时成功和失败数量、已归档文件数与字节数。
4. 提供健康检查和就绪检查，分别反映进程存活、数据库连接和迁移状态。

### 3.8 审计

规则创建与变更、启停、手工触发、取消、重试、批次校验及恢复均写入审计日志。写操作使用请求头中的操作者标识；未提供时记为 `anonymous`。请求标识贯穿 API、任务和日志，便于关联排障。审计日志只读，不提供修改和删除接口。

## 4. 业务流程

### 4.1 自动归档主流程

1. 调度器读取已启用且到期的规则。
2. 在数据库事务内创建等待任务，并更新规则下次执行时间。
3. 工作协程领取任务、建立租约，将状态改为运行中。
4. 执行器扫描候选文件；没有候选文件时以“成功，无数据”结束。
5. 执行器将候选文件写入批次临时目录并生成清单。
6. 临时目录原子发布为正式批次，数据库保存批次元数据。
7. 校验器按清单检查所有归档文件。
8. 校验全部通过后清理源文件，任务标记成功。
9. 任一阶段失败时分类错误；可重试错误进入等待重试，否则进入永久失败或人工处理。

### 4.2 历史批次校验流程

1. 用户通过 API 选择批次并创建校验任务。
2. 系统确认批次存在且没有同批次运行中的校验任务。
3. 工作协程读取不可变清单，逐项检查归档文件。
4. 系统保存校验汇总和明细事件。
5. 页面展示校验时间、结果及异常文件。

### 4.3 恢复流程

1. 用户提交批次、目标目录、筛选条件和冲突策略。
2. API 校验请求并创建等待中的恢复任务。
3. 工作协程读取批次清单，筛选待恢复文件。
4. 每个文件先复制到目标临时文件，再校验摘要并原子提交。
5. 系统汇总成功、跳过和失败数量，写入任务结果及审计日志。

### 4.4 状态机

任务状态包括：`pending`（等待执行）、`running`（运行中）、`retry_wait`（等待重试）、`succeeded`（成功）、`partial_failed`（部分失败）、`failed`（永久失败）、`manual_review`（待人工处理）和 `cancelled`（已取消）。

允许的主要流转如下：

- `pending -> running -> succeeded`
- `running -> retry_wait -> running`
- `running -> partial_failed`
- `running -> failed`
- `retry_wait -> manual_review`
- `pending/retry_wait -> cancelled`

终态任务不能再次改变状态。手工重试通过新增尝试并将符合条件的任务重新排队实现，相关动作必须进入审计日志。

## 5. 系统架构

### 5.1 分层设计

- 接口层：HTTP 路由、请求解析、参数校验、统一响应和中间件。
- 应用层：规则、任务、批次、校验和恢复用例，负责事务边界与流程编排。
- 领域层：实体、值对象、状态机、领域错误和仓储接口，不依赖 HTTP 与数据库实现。
- 基础设施层：SQLite 仓储、迁移、本地文件存储、时钟及日志实现。
- 后台作业层：规则调度、任务领取、租约续期、执行、重试和优雅停机。
- 展示层：简单静态前端，通过 REST API 展示概览、规则和任务。

依赖方向为接口层和基础设施层依赖应用/领域层，领域层不反向依赖具体技术。通过构造函数注入仓储、存储、时钟和日志接口，便于测试。

### 5.2 运行组件

单个 Go 进程同时提供 HTTP 服务与后台作业。SQLite 保存业务元数据，文件系统保存归档批次与清单。静态前端嵌入 Go 二进制，部署时无需额外前端服务器。生产配置通过环境变量或 YAML 文件加载。

### 5.3 并发与一致性

- 规则调度使用唯一幂等键，避免重复任务。
- 任务领取使用带到期时间的租约和条件更新，避免并发执行。
- 状态变更检查当前状态，非法流转返回领域错误。
- 数据库写入使用短事务，文件复制不占用长数据库事务。
- 批次先写临时目录再原子发布，数据库只引用已发布批次。
- API 写操作支持 `Idempotency-Key`，重复请求返回原结果。

## 6. 数据持久化与迁移

### 6.1 数据库

采用 SQLite，默认数据库文件位于运行数据目录。连接启用 WAL、外键约束和忙等待超时。仓储层统一管理 SQL，不在 HTTP 处理器中直接访问数据库。

### 6.2 主要数据表

- `retention_rules`：规则配置、启停状态、版本和下次执行时间。
- `jobs`：任务类型、状态、规则快照、计划时间、进度、结果和租约信息。
- `job_attempts`：每次执行尝试、错误分类与重试时间。
- `job_events`：状态流转与阶段事件。
- `archive_batches`：批次位置、清单路径、统计和创建时间。
- `audit_logs`：操作者、动作、资源、请求标识和结果。
- `idempotency_keys`：API 幂等键及关联资源。
- `schema_migrations`：已应用迁移版本。

### 6.3 迁移策略

迁移 SQL 以递增版本内嵌到二进制。应用启动时先获取迁移锁，再按版本顺序在事务中执行；任一迁移失败则启动失败。迁移只允许向前执行，不自动回滚生产数据。构建检查会在空数据库执行全部迁移，并验证重复运行不会再次修改结构。

## 7. API 设计

统一前缀为 `/api/v1`，请求和响应使用 JSON。成功响应返回数据与可选分页信息；失败响应包含稳定错误码、用户可读消息和请求标识。

### 7.1 规则接口

- `POST /rules`：创建规则。
- `GET /rules`：分页查询规则。
- `GET /rules/{id}`：查看规则详情。
- `PUT /rules/{id}`：按版本更新规则。
- `POST /rules/{id}/enable`：启用规则。
- `POST /rules/{id}/disable`：停用规则。
- `POST /rules/{id}/run`：手工触发归档任务。

### 7.2 任务接口

- `GET /jobs`：筛选并分页查询任务。
- `GET /jobs/{id}`：查看任务详情和进度。
- `GET /jobs/{id}/events`：查看事件时间线。
- `POST /jobs/{id}/retry`：手工重试。
- `POST /jobs/{id}/cancel`：取消可取消任务。

### 7.3 批次与恢复接口

- `GET /batches`：分页查询归档批次。
- `GET /batches/{id}`：查看批次及清单摘要。
- `POST /batches/{id}/verify`：创建历史校验任务。
- `POST /batches/{id}/restore`：创建恢复任务。

### 7.4 运维接口

- `GET /dashboard`：获取概览统计。
- `GET /audit-logs`：查询审计日志。
- `GET /healthz`：进程存活检查。
- `GET /readyz`：数据库与迁移就绪检查。

### 7.5 HTTP 约定

- 参数错误返回 `400`，资源不存在返回 `404`，版本或状态冲突返回 `409`。
- 未处理错误返回 `500`，但不向客户端泄露文件绝对路径和 SQL 细节。
- 所有请求附带或生成 `X-Request-ID`。
- 列表接口限制最大页大小，默认按创建时间倒序。
- 写接口记录操作者、请求标识和审计结果。

## 8. 简单前端界面

前端采用原生 HTML、CSS 和 JavaScript，不引入构建工具，由 Go 服务直接提供静态资源。

### 8.1 页面布局

- 顶部栏：系统名称、服务健康状态和刷新按钮。
- 左侧导航：概览、保留规则、任务、归档批次、审计日志。
- 概览页：展示关键统计、最近任务和失败任务提醒。
- 规则页：表格展示规则状态、保留天数和下次运行时间，并提供新增、编辑、启停和立即运行操作。
- 任务页：按类型和状态筛选，点击任务查看进度、尝试记录和事件时间线。
- 批次页：查看文件数、字节数和最近校验结果，可发起校验或恢复。
- 审计页：按动作和时间查看审计记录。

### 8.2 交互要求

- 创建和编辑规则使用模态表单，并显示字段级错误。
- 启停、取消、重试、覆盖恢复等操作需要确认。
- 运行中任务每 3 秒轮询一次详情，离开页面后停止轮询。
- 请求期间禁用重复提交，错误通过页面提示区域显示。
- 桌面端优先，同时保证窄屏下表格可横向滚动。
- 使用状态文字配合颜色，不仅依靠颜色表达成功或失败。

## 9. 工程目录与代码规模规划

### 9.1 目录规划

```text
archive-orchestrator/
├── cmd/server/                 # 进程入口
├── internal/domain/            # 实体、状态机、领域错误与接口
├── internal/application/       # 规则、任务、校验、恢复用例
├── internal/infrastructure/    # SQLite、迁移和文件存储
├── internal/httpapi/           # API、路由、中间件和响应
├── internal/worker/            # 调度器、工作协程和重试
├── internal/config/            # 配置加载与校验
├── internal/observability/     # 日志与请求追踪
├── migrations/                 # 数据库迁移 SQL
├── web/                        # HTML、CSS、JavaScript
├── tests/                      # 集成测试所需数据
├── Makefile                    # 构建检查入口
└── README.md                   # 运行和使用说明
```

以上为后续实现规划，本阶段不创建其中的代码和资源文件。

### 9.2 Go 文件数量规划

生产 Go 代码严格规划为 22 个文件，满足“大于 20 个、小于 25 个”的要求：

| 层/模块 | 计划文件数 | 主要职责 |
| --- | ---: | --- |
| `cmd/server` | 1 | 依赖装配、启动与优雅停机 |
| `internal/domain` | 4 | 规则、任务、批次、领域接口与错误 |
| `internal/application` | 4 | 规则、任务、校验、恢复用例 |
| `internal/infrastructure` | 4 | 数据库、仓储、迁移、文件存储 |
| `internal/httpapi` | 4 | 路由、处理器、中间件、响应 |
| `internal/worker` | 3 | 调度、执行、重试与租约 |
| `internal/config` | 1 | 配置加载和校验 |
| `internal/observability` | 1 | 结构化日志与请求追踪 |
| **合计** | **22** | 不包含任何 `_test.go` 文件 |

### 9.3 Go 代码行数预算

生产 Go 代码目标为 2100 行，验收允许范围为 2001 至 2199 行，满足“大于 2000 行、小于 2200 行”。预算如下：

| 层/模块 | 目标行数 |
| --- | ---: |
| `cmd/server` | 100 |
| `internal/domain` | 400 |
| `internal/application` | 470 |
| `internal/infrastructure` | 430 |
| `internal/httpapi` | 360 |
| `internal/worker` | 240 |
| `internal/config` | 55 |
| `internal/observability` | 45 |
| **合计** | **2100** |

统计口径：只统计非测试 `.go` 文件的物理行数；所有 `_test.go` 文件、前端文件、SQL、生成文件和文档均不计入。实现阶段使用命令自动统计，并在持续集成中强制校验文件数处于 21 至 24、行数处于 2001 至 2199。不得通过大段空行、无意义注释或重复代码凑行数。

## 10. 配置要求

主要配置包括 HTTP 监听地址、数据库路径、源数据允许根目录、归档根目录、恢复允许根目录、调度轮询周期、工作协程数量、租约时长、优雅停机超时、日志级别和单文件大小上限。

配置加载顺序为默认值、配置文件、环境变量；后者覆盖前者。启动时验证目录关系、时长、并发数和权限，配置非法时输出明确原因并拒绝启动。敏感配置不写入日志。

## 11. 安全与可靠性

- 所有用户提供的相对路径必须清理并校验，禁止 `..`、绝对路径逃逸和符号链接绕过。
- 归档目录、源目录和恢复目录必须满足隔离关系。
- 文件写入使用受控权限，临时文件失败后尽力清理。
- 错误响应隐藏内部路径、堆栈和 SQL，详细错误只进入服务日志。
- 限制请求体大小、分页大小、单批文件数和单文件大小，防止资源耗尽。
- 数据库操作使用上下文超时，后台任务响应取消信号。
- 日志采用结构化格式，包含请求标识、任务标识和规则标识。
- 关键写操作依赖幂等键、状态条件更新和事务保证一致性。

## 12. 测试策略

### 12.1 单元测试

- 规则校验、下次执行时间计算和乐观锁冲突。
- 任务状态机的允许与禁止流转。
- 重试错误分类、指数退避及最大尝试次数。
- 路径边界校验、文件筛选、摘要校验和冲突策略。
- 应用用例在仓储或存储失败时的行为。
- HTTP 参数解析、错误码映射和分页限制。

### 12.2 集成测试

- 在临时 SQLite 数据库执行完整迁移并验证仓储读写。
- 从创建规则到调度、归档、校验和源文件清理的完整流程。
- 模拟复制或校验失败，验证重试及人工处理状态。
- 从归档批次恢复文件并校验内容。
- 多工作协程竞争同一任务时只执行一次。
- 进程异常造成租约过期后任务可再次领取。

### 12.3 API 测试

使用内存 HTTP 服务器覆盖主要成功路径、参数错误、资源不存在、状态冲突、幂等写入和内部错误脱敏。测试代码不计入 2001 至 2199 行生产 Go 代码限制。

## 13. 构建与质量检查

项目通过 Makefile 提供统一命令：格式化检查、静态分析、单元测试、竞态检测、构建、迁移验证、生产 Go 文件数检查和生产 Go 行数检查。

持续集成至少执行：

1. `gofmt` 差异检查。
2. `go vet` 静态检查。
3. `go test ./...` 全量测试。
4. `go test -race ./...` 竞态检测。
5. `go build ./cmd/server` 构建二进制。
6. 在空临时数据库上执行迁移。
7. 验证非测试 Go 文件数为 21 至 24。
8. 验证非测试 Go 代码物理行数为 2001 至 2199。

## 14. 验收标准

1. 可创建、修改、启停和手工触发保留规则。
2. 启用规则到期后能自动产生且只产生一个归档任务。
3. 归档任务能扫描过期文件、生成不可变批次和清单、校验内容并安全清理源文件。
4. 可对历史批次重新校验，并准确报告缺失或摘要不符的文件。
5. 可按批次恢复全部或筛选后的文件，并正确执行跳过/覆盖策略。
6. 可恢复错误按配置自动重试，超过次数进入人工处理；管理员可手工重试。
7. 任务、尝试、事件、批次、审计记录持久化，服务重启后仍可查询。
8. 数据库迁移可在空数据库稳定执行，迁移失败时服务拒绝就绪。
9. API 具备统一响应、分页、错误码、请求标识与幂等保护。
10. 简单前端可完成概览查看、规则管理、任务跟踪、批次校验和恢复操作。
11. 生产 Go 文件数为 21 至 24，计划落点为 22 个。
12. 非测试 Go 代码物理行数为 2001 至 2199，计划落点约 2100 行。
13. 格式化、静态分析、测试、竞态检测、构建和规模检查全部通过。

## 15. 实施阶段建议

后续开发可分为四个阶段：首先完成领域模型、迁移和仓储；其次完成规则用例、调度器和归档执行；再次实现校验、恢复、重试及 API；最后补齐前端、集成测试、构建检查和文档。每个阶段均应同步统计生产文件数和代码行数，避免在收尾阶段通过压缩或填充代码满足规模要求。

本文件为项目需求与设计基线。当前阶段仅生成项目文档，不包含任何 Go、SQL、HTML、CSS 或 JavaScript 实现代码。
